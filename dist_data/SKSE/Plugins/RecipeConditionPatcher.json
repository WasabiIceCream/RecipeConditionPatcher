{
  "//": "Full documentation of this file's format is in the README. The default material->perk mappings ship separately in MatsNeedPerks_RCP.json (see the README's 'Adding your own config as a separate mod' section); this file only holds settings and an example recipeOverrides entry.",

  "enabled": true,
  "existingPerkMode": 0,
  "logLevel": 0,

  "recipeOverrides": [
    {
      "recipe": "SomeExampleRecipeEditorID",
      "exclude": true,
      "comment": "This entry is just an example. 'exclude' being true for this recipe will prevent RCP from making any changes, unless another config overrides it again"
    }
  ]
}
